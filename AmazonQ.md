# Amazon 'q' Bootstrap Instructions

This file is automatically read if present in the directory where `q` starts.

Place `q`-specific instructions here.

Note that `q` automatically reads AGENTS.md and README.md, so you do not need to explictly reference them here.

